import React from 'react';

const head = (
    <div>

    </div>
)
const body = (
    <div>

    </div>
)
const leftArm = (
    <div>

    </div>
)
const rightArm = (
    <div>

    </div>
)
const leftFoot = (
    <div>

    </div>
)
const rightFoot = (
    <div>

    </div>
)

const bodyParts = [
    head,
    body,
    leftArm,
    rightArm,
    leftFoot,
    rightFoot
]

type ManProps = {
    numberOfMistakes: number
}

const Man = ({numberOfMistakes}: ManProps) => {
    return (
        <div>

        </div>
    );
};

export default Man;